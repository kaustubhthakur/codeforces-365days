#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        long long a,b,c;
        cin>>a>>b>>c;
        long long one = a,two = 2*b,three = 3*c;
       cout<<(a+c)%2<<endl;
    }
    return 0;
}