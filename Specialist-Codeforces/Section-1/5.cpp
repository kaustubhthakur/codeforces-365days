#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        string str;
        cin>>str;
        int n = str.size();
        for(int i=0;i<n;i++)
        {
            if(i%2==0 && str[i]==str[i%2==0])
            {
                
            }
        }

    }
    return 0;
}